import React, { useState, useContext } from 'react';
import { observer } from 'mobx-react-lite';
import './DopuskProf.css';

const DopuskProf = observer(() => {
    return (
        <div> html код писать тут </div>
    );
});

export default DopuskProf;